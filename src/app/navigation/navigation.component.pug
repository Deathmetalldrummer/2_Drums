mat-sidenav-container.sidenav-container
  .mat-drawer-backdrop.ng-star-inserted
  mat-sidenav.sidenav(#drawer='', fixedinviewport='true', [attr.role]="(isHandset$ | async) ? 'dialog' : 'navigation'", [mode]="(isHandset$ | async) ? 'over' : 'side'", [opened]='!(isHandset$ | async)')
    mat-toolbar Menu
    mat-nav-list
      a(mat-list-item='' href='#') Link 1
      a(mat-list-item='' href='#') Link 2
      a(mat-list-item='' href='#') Link 3
  mat-sidenav-content
    mat-toolbar(color='primary')
      button(type='button', aria-label='Toggle sidenav', mat-icon-button='', (click)='drawer.toggle()')
        mat-icon(aria-label='Side nav toggle icon') menu
      span MaterialAngular
    // Add Content Here
